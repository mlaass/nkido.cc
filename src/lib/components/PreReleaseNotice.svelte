<div class="pre-release-wrap">
	<aside class="pre-release-notice" role="note">
		<strong>NKIDO is pre-1.0.</strong>
		Syntax and features may change, things may not always work as expected, and the docs can lag behind. Treat <a href="https://live.nkido.cc" target="_blank" rel="noopener">live.nkido.cc</a> as the source of truth for current behavior — see <a href="/docs/stability">stability &amp; expectations</a> for the policy.
	</aside>
</div>

<style>
	.pre-release-wrap {
		padding: 0 var(--content-padding);
		display: flex;
		justify-content: center;
	}

	.pre-release-notice {
		max-width: 700px;
		width: 100%;
		padding: var(--spacing-sm) var(--spacing-md);
		margin: var(--spacing-lg) 0;
		background: var(--bg-tertiary);
		border: 1px dashed var(--accent-warning, var(--border-default));
		border-radius: 8px;
		color: var(--text-secondary);
		font-size: 0.875rem;
		line-height: 1.5;
	}

	.pre-release-notice strong {
		color: var(--text-primary);
	}
</style>
